# The Pendulum

The maintained interactive simulation now lives in [`ssc-tools`](https://github.com/pportela1980/ssc-tools/tree/main/simulations/pendulum) and is published at [tools.systemsandcomplexity.school](https://tools.systemsandcomplexity.school/simulations/pendulum/).

This repository remains published so existing GitHub Pages links redirect to the maintained page. Its Git history preserves the earlier source.

The original `index` file is an older variant; it is preserved at the canonical repository as `simulations/pendulum/legacy.html`.
